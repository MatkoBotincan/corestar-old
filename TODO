Immediate tasks:
- Generate unit tests from examples in playground
- Add test input files for abduction
- Add unit tests for abduction
- Sort out the documentation and prepare coreStar tutorial
- Sort out loading of logic files (location, ...)
- Rename JSTAR_SMT_PATH and JSTAR_SMT_ARGUMENTS
- Rename MULT to STAR, and add Mult as a binop
- Rename lex.OR to lex.BAR
- Rename lex.MULT to lex.STAR
- TODO(rgrig): Functions in Vars should be used instead of new*Var. (Parser.mly)
- Sort out spec parameter handling
- Token information passing to core files
- Check whether precedence is needed in parser
- Check translation of parser.equiv_rule

To think about after refactoring:
- Exceptions handling
- Sharing support syntax between smt, ai, ...
- Support for quoting of external syntax
- Revisit rewrite rules distinction (saturate=true,false);
